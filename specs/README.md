# Specs

本目录现在只保留当前仍有运行价值的契约文件。

当前应优先参考：

- `001-build-oneall-platform/contracts/openapi.yaml`

说明：

- 该契约文件仍被后端 contract tests 直接引用
- 其它原始规格、计划、研究、任务拆分等历史文档，已归档到 `docs/archive/specs/`
- 当前项目真相源仍然以仓库根目录 `README.md` 与现行源码为准
