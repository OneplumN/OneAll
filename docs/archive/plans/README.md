# Archived Plans

本目录保存已完成阶段的历史设计稿、实施计划和过程文档。

这些文件的特点是：

- 主要用于追溯当时的设计背景、拆分方式和执行顺序
- 可能引用已经删除的模块、旧目录名、旧命令或过期路径
- 不应作为当前仓库结构和运行方式的权威说明

当前实现请优先参考：

- 仓库根目录 `README.md`
- `docs/plans/` 下的最新审计和阶段收官文档
- `src/`、`backend/src/`、`probes/` 下的现行代码
- `specs/001-build-oneall-platform/contracts/openapi.yaml`
