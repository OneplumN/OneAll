This placeholder reserves the OneAll UI style guide deliverable.

Use the shared Figma project “OneAll Platform — UI Library” (file key TBD) to maintain the official `.fig` binary.
Export the latest revision to this path when available.
